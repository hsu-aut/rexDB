# Import of custom type to account for dynamic key
scalar SparqlResultEntry,

type SparqlQueryResult {
	head: HeadObject,
	results: ResultObject
}

type HeadObject {
	vars: [String]
}

type ResultObject {
	bindings: [SparqlResultEntry]
}


type Query {
	sparqlQuery(queryString: String!): SparqlQueryResult
}

